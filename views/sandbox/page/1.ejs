<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube OAuth 認証</title>
    <link rel="stylesheet" href="/css/page.css">
    <script src="/js/tailwindcss.js"></script>
</head>
<body class="bg-gray-900 text-white">
    <header class="bg-gray-800 p-4 flex justify-between items-center">
        <h1 class="text-xl font-bold">認証</h1>
    </header>
  
    <div class="container mx-auto p-6 flex flex-col items-center">
        <div class="bg-gray-800 p-6 rounded-lg shadow-lg w-full max-w-md">
            <h2 class="text-xl font-bold mb-4">アカウントに接続</h2>
            <p class="text-gray-400 mb-4">ありがとう。もう少しで完了です！<br>接続ボタンを押して、指定されるコードを入力してください。</p>
            
            <div class="flex items-center space-x-4 mb-6">
                <img src="https://cdn.glitch.global/5b8b419f-e61e-4533-9e15-5b2805b88d0e/IMG_1111_Original.jpeg" alt="わかめtube" class="w-12 h-12 rounded-full">
                <p class="text-lg">わかめtube</p>
            </div>
  
        <button id="auth-button" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg font-semibold">YouTubeに接続</button>

            <a href="/" class="block text-center text-gray-400 mt-4 hover:text-gray-300">キャンセル</a>
        </div>
    </div>
    
    <script>
    document.getElementById("auth-button").addEventListener("click", async () => {
        const res = await fetch("/auth/start");
        const data = await res.json();
        alert(`認証ページにアクセスしてコードを入力してください: ${data.verification_url}`);
    });
    </script>
</body>
</html>