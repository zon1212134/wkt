<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube OAuth 認証</title>
    <link rel="stylesheet" href="/css/page.css">
    <script src="/js/tailwindcss.js"></script>
</head>
<body class="bg-gray-900 text-white">
    <header class="bg-gray-800 p-4 flex justify-between items-center">
        <h1 class="text-xl font-bold">認証</h1>
    </header>
    
    <main class="container mx-auto p-6 text-center">
        <p class="text-lg mb-4">YouTubeにログインして、サービスを利用するための認証を行ってください。</p>
        <button id="auth-button" class="px-6 py-3 bg-blue-600 text-white text-lg font-semibold rounded-full shadow-md hover:bg-blue-700 transition">YouTubeにログイン</button>
        <button id="check-auth" class="ml-4 px-6 py-3 bg-gray-600 text-white text-lg font-semibold rounded-full shadow-md hover:bg-gray-700 transition">認証情報を確認</button>
    </main>
    
    <script>
    document.getElementById("auth-button").addEventListener("click", async () => {
        const res = await fetch("/auth/start");
        const data = await res.json();
        alert(`認証ページにアクセスしてコードを入力してください: ${data.verification_url}`);
    });
    
    document.getElementById("check-auth").addEventListener("click", async () => {
        const res = await fetch("/auth/info");
        const data = await res.json();
        console.log("認証情報:", data);
        alert(JSON.stringify(data, null, 2));
    });
    </script>
</body>
</html>


<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>認証 - わかめtube</title>
    <link rel="stylesheet" href="/css/page.css">
    <script src="/js/tailwindcss.js"></script>
</head>
<body class="bg-gray-900 text-white">
    <header class="bg-gray-800 flex p-4 justify-between items-center">
        <h1 class="text-lg font-bold">わかめtube 認証</h1>
    </header>
    
    <div class="container mx-auto p-6 flex flex-col items-center">
        <div class="bg-gray-800 p-6 rounded-lg shadow-lg w-full max-w-md">
            <h2 class="text-xl font-bold mb-4">アカウントに接続</h2>
            <p class="text-gray-400 mb-4">このアプリにアカウントへのアクセスを許可しますか？</p>
            
            <div class="flex items-center space-x-4 mb-6">
                <img src="/img/default-avatar.png" alt="ユーザーアイコン" class="w-12 h-12 rounded-full">
                <p class="text-lg">あなたのアカウント</p>
            </div>
            
            <form action="/oauth/authorize" method="POST">
                <input type="hidden" name="client_id" value="<%= client_id %>">
                <input type="hidden" name="redirect_uri" value="<%= redirect_uri %>">
                <input type="hidden" name="response_type" value="code">
                
                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg font-semibold">許可する</button>
            </form>
            
            <a href="/" class="block text-center text-gray-400 mt-4 hover:text-gray-300">キャンセル</a>
        </div>
    </div>
</body>
</html>