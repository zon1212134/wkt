<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>動画取得エラー</title>
    <script>
        function changeServer() {
            const server = document.getElementById('serverSelect').value;
            window.location.href = `?server=${server}`;
        }
    </script>
    <style>
        body {
            background-color: #1a1a1a;
            color: white;
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: auto;
            background: #2a2a2a;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.1);
        }
        img {
            width: 100%;
            border-radius: 5px;
        }
        select, button {
            margin-top: 10px;
            padding: 10px;
            font-size: 16px;
            background: #444;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>動画を取得できません</h1>
        <p>エラー: <%= error %></p>
        <img src="https://img.youtube.com/vi/<%= videoId %>/maxresdefault.jpg">
        <p>現在のサーバー: <strong><%= baseUrl %></strong></p>
        <label for="serverSelect">サーバーを変更:</label>
        <select id="serverSelect">
            <option value="server8">server8</option>
            <option value="server12">server12</option>
            <option value="server24">server24</option>
            <option value="backup">backup</option>
        </select>
        <button onclick="changeServer()">変更</button>
    </div>
</body>
</html>
