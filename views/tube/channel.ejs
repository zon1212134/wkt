<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>適当なチャンネル - チャンネル</title>
    <link rel="stylesheet" href="/css/page.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-4 pt-24">
        <!-- カバー画像 -->
        <div class="w-full h-48 bg-gray-700 flex items-center justify-center text-xl">カバー画像</div>
        
        <!-- チャンネル情報 -->
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg mt-4">
            <div class="flex items-center mb-6">
                <img src="https://via.placeholder.com/80" class="w-20 h-20 rounded-full mr-4">
                <div>
                    <h1 class="text-3xl font-bold">適当なチャンネル</h1>
                    <p class="text-gray-400 text-sm">これは適当なチャンネルの説明です。</p>
                    <p class="text-gray-500 text-sm mt-2">100万 subscribers • 500 videos</p>
                </div>
            </div>
        </div>
        
        <!-- タブナビゲーション -->
<div class="tabber">
  <div class="tab-buttons flex space-x-4 border-b">
    <% shelves.forEach((shelf, index) => { %>
      <button class="tab-button px-4 py-2 text-white bg-gray-700 hover:bg-gray-600" data-tab="tab-<%= index %>">
        <%= shelf.title.text %>
      </button>
    <% }); %>
  </div>

  <div class="tab-contents">
    <% shelves.forEach((shelf, index) => { %>
      <div class="tab-content p-4 <%= index === 0 ? '' : 'hidden' %>" id="tab-<%= index %>">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% shelf.content?.items?.forEach(video => { %>
            <div class="video-item bg-gray-800 p-4 rounded">
              <a href="/wkt/watch/<%= video.id %>">
                <img loading="lazy" src="<%= video.thumbnail %>" alt="<%= video.title %>" class="w-full h-48 rounded">
                <span class="video-duration"><%= video.duration %></span>
              </a>
              <p class="text-lg font-bold mt-2"><%= video.title %></p>
              <p class="text-xs text-gray-500"><%= video.views %>回視聴 - <%= video.published %></p>
            </div>
          <% }); %>
        </div>
      </div>
    <% }); %>
  </div>
</div>
    </div>
    
    <script>
        $(document).ready(function() {
            $('.tab-button').click(function() {
                $('.tab-button').removeClass('text-white border-blue-500').addClass('text-gray-400');
                $(this).addClass('text-white border-blue-500');
                let tab = $(this).data('tab');
                $.get(`/api/channel/videos?tab=${tab}`, function(data) {
                    $('#video-container').html(data.html);
                });
            });
        });
    </script>
</body>
</html>
