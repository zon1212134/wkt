<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>適当なチャンネル - チャンネル</title>
    <link rel="stylesheet" href="/css/page.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-4 pt-24">
        <!-- カバー画像 -->
        <div class="w-full h-48 bg-gray-700 flex items-center justify-center text-xl">カバー画像</div>
        
        <!-- チャンネル情報 -->
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg mt-4">
            <div class="flex items-center mb-6">
                <img src="https://via.placeholder.com/80" class="w-20 h-20 rounded-full mr-4">
                <div>
                    <h1 class="text-3xl font-bold">適当なチャンネル</h1>
                    <p class="text-gray-400 text-sm">これは適当なチャンネルの説明です。</p>
                    <p class="text-gray-500 text-sm mt-2">100万 subscribers • 500 videos</p>
                </div>
            </div>
        </div>
        
        <!-- タブナビゲーション -->
        <div class="flex space-x-4 mt-6 border-b border-gray-600">
            <button class="tab-button px-4 py-2 text-white border-b-2 border-transparent" data-tab="latest">最新</button>
            <button class="tab-button px-4 py-2 text-gray-400 border-b-2 border-transparent" data-tab="popular">人気</button>
            <button class="tab-button px-4 py-2 text-gray-400 border-b-2 border-transparent" data-tab="shorts">ショート</button>
        </div>

        <!-- 動画リスト -->
        <div id="video-container" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 mt-6">
            <% channel.current_tab?.content?.contents?.forEach(video => { %>
                <% video.contents.forEach(video => { %>
                    <%- include("back/videolist.ejs", { video }); %>
                <% }); %>
            <% }); %>
            <% videos.current_tab?.content?.contents?.forEach(video => { %>
                <%- include("back/videolist.ejs", { video: video.content }); %>
            <% }); %>
        </div>
    </div>
    
    <script>
        $(document).ready(function() {
            $('.tab-button').click(function() {
                $('.tab-button').removeClass('text-white border-blue-500').addClass('text-gray-400');
                $(this).addClass('text-white border-blue-500');
                let tab = $(this).data('tab');
                $.get(`/api/channel/videos?tab=${tab}`, function(data) {
                    $('#video-container').html(data.html);
                });
            });
        });
    </script>
</body>
</html>
